'use strict';

angular.module('scholarshipSystemClientApp')
  .controller('UserProfileController', ['$scope', '$location', 'ValidationService', function ($scope, $location, validationService) {
  }]);
